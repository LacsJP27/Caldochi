{
  "name": "caldochi",
  "private": true,
  "workspaces": [
    "web",
    "api"
  ],
  "scripts": {
    "dev:api": "npm --workspace @caldochi/api run dev",
    "build": "npm run build --workspaces",
    "test": "npm --workspace @caldochi/api test",
    "typecheck": "npm --workspace @caldochi/api run typecheck"
  }
}
